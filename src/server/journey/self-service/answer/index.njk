{% extends "layouts/page.njk" %}

{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}

{% block govukServiceNavigation %}{% endblock %}

{% block beforeContent %}{% endblock %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-full app-iat-answers-page">
      <h1 class="govuk-heading-xl">{{ heading }}</h1>

      <h2 class="govuk-heading-m">Date of check</h2>
      <p class="govuk-body">{{ dateOfCheck | formatDate }}</p>

      <h2 class="govuk-heading-m">Summary</h2>
      <p class="govuk-body">{{ summaryText }}</p>

      <h2 class="govuk-heading-m">Your answers</h2>
      {% set rows = [] %}
      {% for entry in answers %}
        {% set valueHtml %}
          {% if entry.answers | length > 1 %}
            <ul class="govuk-list govuk-list--bullet">
              {% for a in entry.answers %}
                <li>{{ a.text }}</li>
              {% endfor %}
            </ul>
          {% else %}
            {{ entry.answers[0].text }}
          {% endif %}
        {% endset %}
        {% set rows = (rows.push({ key: { text: entry.questionText }, value: { html: valueHtml } }), rows) %}
      {% endfor %}

      {{ govukSummaryList({ rows: rows }) }}

      <p class="govuk-body">
        <a class="govuk-link print-link"
           href="#"
           data-module="iat-answer-print">
          Print this page
        </a>
      </p>
    </div>
  </div>
{% endblock %}
