{% from "govuk/components/button/macro.njk" import govukButton %}

<h1 class="govuk-heading-xl">{{ heading }}</h1>

{% if body %}
  <div class="govuk-body">{{ body | safe }}</div>
{% endif %}

<div class="app-iat-actions">
  {% if hasContinue %}
    {{ govukButton({
      text: ctaLabel,
      href: "#",
      classes: "govuk-!-margin-right-3"
    }) }}
  {% endif %}
</div>

{% if answerPageUrl %}
  <p class="govuk-body">
    <a class="govuk-link"
       href="{{ answerPageUrl }}"
       target="_blank"
       rel="noopener noreferrer">
      View answers<span class="govuk-visually-hidden"> (opens in a new tab)</span>
    </a>
  </p>
{% endif %}
