{% extends "layouts/page.njk" %}
{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/details/macro.njk" import govukDetails %}
{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}
{% from "govuk/components/fieldset/macro.njk" import govukFieldset %}
{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "cancel-button/macro.njk" import appCancelButton %}

{% block pageTitle %}{{ pageTitle }}{% endblock %}

{% set detailsSummary = "Help with eastings and northings formats" %}
{% set detailsHTML %}
<p class="govuk-body">
  <strong>OSGB36</strong>
  (also known as the British National Grid) is a system used to map Great Britain.
</p>
<p class="govuk-body">
  It divides the country into squares using numerical east and north positions, known as eastings and northings.
</p>
<p class="govuk-body">
  Eastings tell you how far east a place is, and northings tell you how far north it is.
</p>
{% endset %}
{% block beforeContent %}
  {{ govukBackLink({
    text: "Back",
    href: backLink
  }) }}
{% endblock %}
{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      {% if errorSummary %}
        {{ govukErrorSummary({
          titleText: "There is a problem",
          errorList: errorSummary
        }) }}
      {% endif %}
      <span class="govuk-caption-l">{{ projectName }}</span>
      <h1 class="govuk-heading-l">{{ heading }}</h1>
      <p>Eastings and northings should only include numbers. For example: 123456, 654321.</p>
      {{ govukDetails({
        summaryText: detailsSummary,
        html: detailsHTML
      }) }}
      <form method="POST">
        <input type="hidden" name="csrfToken" value="{{ csrfToken }}">
          <input type="hidden" name="coordinateSystem" value="OSGB36">
            {% for i in range(0, 3) %}
              {% set coordinate = coordinates[i] or { eastings: '', northings: '' } %}
              {% set pointNumber = i + 1 %}
              {% set legendText %}
              {% if i == 0 %}Start and end point{% else %}Point {{ pointNumber }}
              {% endif %}
              {% endset %}
              {% call govukFieldset({
            legend: {
              text: legendText,
              classes: "govuk-fieldset__legend--m"
            }
          }) %}
              {{ govukInput({
              id: "coordinates[" + i + "][eastings]",
              name: "coordinates[" + i + "][eastings]",
              value: coordinate.eastings,
              errorMessage: errors["coordinates" + i + "eastings"] if errors,
              label: {
                text: "Eastings",
                classes: "govuk-!-font-weight-bold"
              },
              classes: "govuk-!-width-one-half"
            }) }}
              {{ govukInput({
              id: "coordinates[" + i + "][northings]",
              name: "coordinates[" + i + "][northings]",
              value: coordinate.northings,
              errorMessage: errors["coordinates" + i + "northings"] if errors,
              label: {
                text: "Northings",
                classes: "govuk-!-font-weight-bold"
              },
              classes: "govuk-!-width-one-half"
            }) }}
              {% endcall %}
            {% endfor %}
            <div class="govuk-button-group govuk-!-margin-top-8">
              {{ govukButton({
            text: "Continue",
            attributes: {
              id: "continue"
            }
          }) }}
              {{ appCancelButton({
            cancelLink: "/exemption/task-list?cancel=site-details"
          }) }}
            </div>
          </form>
        </div>
      </div>
    {% endblock %}