# CLAUDE.md

## Code Standards

- Use the rules in the .cursor/rules folder
- Don't add code comments, unless there is complex logic to explain
- When writing unit tests, minimise mocking of sub-dependencies, and if mocking is required, do it at the lowest level of sub-dependency
